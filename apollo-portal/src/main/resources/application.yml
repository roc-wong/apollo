spring:
  application:
    name: apollo-portal
  profiles:
    active: ${apollo_profile}
  jpa:
    properties:
      hibernate:
        query:
          plan_cache_max_size: 192 # limit query plan cache max size
  cloud:
    inetutils:
      ignored-interfaces:
      - docker0
      - veth.*
  mail:
    host: smtp.qq.com
    username: roc.wong@foxmail.com
    password: nosfmvajopfhbbca
    port: 465
    properties:
      mail:
        smtp:
          auth: true
          connectiontimeout: 5000
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
          starttls:
            enable: true
            required: true
          timeout: 5000
        transport:
          protocol: smtp


server:
  port: 8070
  compression:
    enabled: true
  tomcat:
    use-relative-redirects: true

logging:
  file: /opt/logs/100003173/apollo-portal.log

management:
  health:
    status:
      order: DOWN, OUT_OF_SERVICE, UNKNOWN, UP


